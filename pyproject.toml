[project]
name = "biblioteca-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.2",
    "sqlalchemy>=2.0.51",
    "uvicorn>=0.51.0",
    "httpx>=0.28.1",
    "pydantic-settings>=2.8.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
typeCheckingMode = "standard" # ou "basic" / "strict"
